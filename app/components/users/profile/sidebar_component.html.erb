<%=
  component_wrapper do
    render(Primer::OpenProject::BorderGrid.new(spacious: true)) do |border_grid|
      border_grid.with_row { render(Users::Profile::AttributesComponent.new(user: @user)) }
      border_grid.with_row { render(Users::Profile::GroupsComponent.new(user: @user)) }
      border_grid.with_row { render(Users::Profile::ProjectsComponent.new(user: @user)) }
    end
  end
%>
